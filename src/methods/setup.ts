import { Dolibarr } from "../dolibarr.class"

export function setup(this: Dolibarr) {
	const checkIntegrity = null

	const getCompanyProperties = null

	const getSetupVariable = null

	const deliveryTimesList = null

	const civilitiesList = null

	const contactsTypesList = null

	const countriesList = null

	const getCountry = null

	const getCountryByCode = null

	const getCountryByIso = null

	const CurrenciesList = null

	const eventsTypesList = null

	const expenseReportTypesList = null

	const legalFormBusinessList = null

	const orderingMethodsList = null

	const orderingOriginsList = null

	const paymentsTermsList = null

	const paymentsTypesList = null

	const shippingMethodsList = null

	const socialNetworksList = null

	const staffList = null

	const statesProvincesList = null

	const getState = null

	const getStateByCode = null

	const ticketsCategoriesList = null

	const ticketsSeverityList = null

	const ticketsTypesList = null

	const townsList = null

	const measuringUnitsList = null

	const establishmentsList = null

	const getEstablishment = null

	const getEstablishmentById = null

	const extraFieldsList = null

	const enabledModulesList = null

	return {
		checkIntegrity,
		getCompanyProperties,
		getSetupVariable,
		deliveryTimesList,
		civilitiesList,
		contactsTypesList,
		countriesList,
		getCountry,
		getCountryByCode,
		getCountryByIso,
		CurrenciesList,
		eventsTypesList,
		expenseReportTypesList,
		legalFormBusinessList,
		orderingMethodsList,
		orderingOriginsList,
		paymentsTermsList,
		paymentsTypesList,
		shippingMethodsList,
		socialNetworksList,
		staffList,
		statesProvincesList,
		getState,
		getStateByCode,
		ticketsCategoriesList,
		ticketsSeverityList,
		ticketsTypesList,
		townsList,
		measuringUnitsList,
		establishmentsList,
		getEstablishment,
		getEstablishmentById,
		extraFieldsList,
		enabledModulesList,
	}
}
